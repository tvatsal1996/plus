#include <iostream>
using namespace std;
// #include <map>
#include "game.h"

int main() {
    Game game(chessBoard, Move(Field("a2"), Field("a3"), false), 'w', "Just started");
    while(1){
        
    }
    return 0;
}